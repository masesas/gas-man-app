import 'package:flutter/material.dart';

const kTeal = Color(0xFF0E827C);
const kAmber = Color(0xFFFFC970);
const kText = Color(0xFF1D2A2A);

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0E827C); // Teal
  static const Color accent = Color(0xFFFFC970); // Amber
  static const Color surface = Color(0xFFF6F7F8); // Light grey background
  static const Color onPrimary = Colors.white;
  static const Color text = Color(0xFF1D2A2A);
}
